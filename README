INSTALLATION of module
======================

 $> ./configure --add-module=/path/to/ngx_http_accounting_module
 $> make
 #> make install


CONFIGURATION in nginx
======================
 http{
  http_accounting  on;   # open accounting function
  ...
  
  server {
    http_accounting_id  accounting_id_str;   # set accounting_id
    ...

    location / {
      http_accounting_id  accounting_id_str;    # set accounting_id
      ...
    
    }
  }
 }
